$( "#ui-dialog" ).html('<%= escape_javascript(render :partial => 'form') %>')
$( "#ui-dialog" ).dialog({
      height: 350,
      width: 550,
      title: '<%= t(".title").html_safe %>',
      modal: false
  });
initFormStyles();
$( "#ui-dialog" ).open();
